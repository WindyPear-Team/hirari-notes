# Hirari Notes 移动端设计方案

## 产品定位

Hirari Notes 是一款以本地优先为基础的结构化便签工具。用户可自行定义便签格式，例如“读书摘录”“资产条目”或“工作复盘”，并将每条内容序列化为可同步的数据文档。所有普通便签和结构化条目均先保存至设备本地；同步功能仅负责将标准化数据包在用户选择的远端位置之间交换。

## 核心架构决策

应用采用本地优先的数据流。记录、格式定义与同步配置均有唯一 ID、设备 ID、递增修订号、修改时间与内容摘要。同步以“清单 + 实体”文档形式传输，先交换清单、再按需传输变化实体。凭据只放在系统安全存储中，且不会包含在格式导出文件中。

| 同步方式 | 首版交互与数据契约 | 实现边界 |
| --- | --- | --- |
| WebDAV | 通过 HTTPS 请求远端目录中的清单和实体 JSON 文档 | 可由应用网络层直接执行；需用户提供支持 WebDAV 的服务地址和凭据。 |
| SFTP | 使用相同的清单与实体文档契约 | 需要支持 SSH 的原生客户端能力；在最终原生构建中启用，开发预览保留完整配置与适配接口。 |
| 自定义 HTTP | 用户通过可视化流程组合请求、发送、响应解析、字段映射和错误处理节点；系统把输出映射为标准同步包 | 仅支持声明式节点与受限表达式，不在设备上执行任意 JavaScript，从而避免凭据和本地数据被脚本滥用。 |
| 手动导入导出 | 生成或读取签名无关的 JSON 归档文件 | 用于离线迁移与备份；可选择仅导出格式定义、仅导出数据，或两者一起导出。 |

冲突处理默认采用字段级三方合并：若两端修改的字段不重叠，自动合并；若同一字段都变化，则依据用户可配置策略选择“保留本机”“保留远端”“保留较新版本”或“创建冲突副本”。任何自动处理都会写入可查看、可回滚的冲突记录。

## 屏幕清单

| 屏幕 | 主要内容 | 关键操作 |
| --- | --- | --- |
| 便签库 | 搜索、最近条目、格式筛选、同步状态 | 新建便签、打开记录、快速同步。 |
| 条目编辑器 | 动态生成的字段表单、元数据、保存状态 | 填写字段、验证必填项、保存或删除。 |
| 格式库 | 已定义的格式卡片与字段数量 | 新建、复制、编辑、导入或导出格式定义。 |
| 格式设计器 | 标题、图标、字段类型、必填、校验、默认值与显示排序 | 添加字段、拖动排序、保存并预览。 |
| 同步中心 | 远端列表、最近同步、待处理变更、错误摘要 | 新增 WebDAV/SFTP/HTTP 同步目标、立即同步、查看日志。 |
| 同步目标编辑器 | 协议、地址、认证、路径、请求/响应映射、冲突策略 | 测试配置、保存为启用或停用状态。 |
| HTTP 流程编辑器 | 节点画布、连线、节点属性面板、流程校验和运行预览 | 新增节点、连接步骤、配置映射、查看测试输出。 |
| 冲突记录 | 自动合并结果和需人工处理的字段差异 | 比较本机/远端值、选择结果、恢复副本。 |
| 设置与备份 | 设备标识、自动同步频率、导入导出与安全选项 | 导出归档、导入归档、清理本地数据。 |

## 关键用户流程

### 创建并使用自定义格式

用户在“格式库”点击新增格式，输入格式名称并添加所需字段。保存后，便签库的新增操作会显示该格式。用户填写条目后，应用按照格式定义生成结构化 JSON，并记录格式版本以确保日后字段演进可追踪。

### 导出并导入格式定义

用户在格式详情中点击导出，选择仅导出该格式或连同引用的条目一并导出。导出文件包含格式 ID、版本、字段架构与显示设置，但不包含同步凭据。另一设备导入后先做架构校验：相同 ID 且版本更新时提供替换、保留两个版本或取消三种选择；无冲突时直接加入格式库。

### 配置并执行同步

用户从同步中心选择协议并填写远端信息。WebDAV 和 SFTP 使用统一目录契约；自定义 HTTP 打开流程编辑器，用户以“开始 → 构造请求 → 发送请求 → 解析 JSON → 映射同步包 → 结束”形式定义发送与接收过程。每个节点的属性在底部抽屉中编辑，连线定义成功或错误分支。保存前系统校验流程是否有起止节点、是否存在断开连线，以及最终输出是否符合标准同步包。保存后可运行连接测试。每次同步都先比较清单，再上传、下载或合并变化；完成后显示成功数量、冲突数量与时间。

### 自动冲突解决

当同一条目在多端被修改时，应用以最近共同修订版本为基准逐字段比较。如果字段只在一端改动，自动采用该值；同一字段双向改动时按冲突策略决策。不可自动决策时创建带来源信息的冲突副本，保证任何一端内容不会被静默覆盖。

## 视觉与交互设计

界面以 9:16 竖屏和单手操作为基准，遵循 iOS 信息层级与导航习惯：顶部使用大标题与明确上下文，主要操作固定于拇指可触达的底部区域，设置项使用分组列表，危险操作需要二次确认。编辑器采用原生表单节奏，字段标签与帮助文字紧密关联，敏感字段默认隐藏。

| 设计元素 | 选择 | 用途 |
| --- | --- | --- |
| 品牌主色 | `#275D54` 深松绿 | 主操作、状态强调与品牌识别。 |
| 辅助强调 | `#E4A853` 琥珀金 | 同步状态、提示与格式标签。 |
| 浅色背景 | `#F7F8F5` 雾白 | 长时间阅读时的低刺激背景。 |
| 深色背景 | `#111816` 墨绿黑 | 深色模式下的沉稳层级。 |
| 成功状态 | `#2D7B58` | 同步成功、校验通过。 |
| 风险状态 | `#B5473D` | 冲突、删除与连接错误。 |

## 数据模型摘要

`NoteSchema` 描述一个自定义格式，包含字段定义、显示配置和格式版本。`NoteRecord` 存储标准元数据和符合架构的 `data` 对象。`SyncProfile` 描述一个远端及其协议参数，但引用的敏感凭据仅以安全存储键标识。`SyncManifest` 汇总各实体的修订、摘要和删除墓碑。`ConflictRecord` 记录合并依据、字段级决议和恢复信息。

所有可以导入导出的文件都以 `schemaVersion` 包装；导入时先校验版本与字段类型，再写入本地存储。该设计允许未来添加更多字段类型与同步协议，同时保持旧数据可读取。

## HTTP 流程编辑器节点约束

可视化流程不是通用编程环境，而是同步映射的可审计配置。节点只能读取上游节点的具名输出与已授权的配置变量；敏感值通过 `credentialRef` 间接引用，画布和日志均不会回显明文凭据。流程可导出为 JSON 定义，便于备份、共享和审查。

| 节点 | 输入 | 输出 | 作用 |
| --- | --- | --- | --- |
| 开始 | 同步方向、清单与变化集 | 标准流程上下文 | 规定流程入口与可用变量。 |
| 构造请求 | URL 模板、方法、头、正文模板 | HTTP 请求对象 | 从配置和上下文安全地构造请求。 |
| 发送请求 | HTTP 请求对象 | 状态码、头与正文 | 执行一次网络请求，并将非成功状态导向错误分支。 |
| 解析响应 | 文本或 JSON 正文 | 命名对象 | 使用 JSONPath 或固定字段读取响应内容。 |
| 映射同步包 | 解析对象 | 标准同步包 | 映射为清单、实体、游标或确认信息。 |
| 条件分支 | 状态、字段存在性或空值 | 成功/失败端口 | 按受限条件选择后续路径。 |
| 错误结束 | 错误码与说明 | 可读同步日志 | 中止本次同步并保留可诊断信息。 |
| 成功结束 | 标准同步包 | 可应用的同步结果 | 提交给同步引擎进行验证和合并。 |
